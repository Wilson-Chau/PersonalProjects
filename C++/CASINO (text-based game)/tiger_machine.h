#ifndef TIGER_MACHINE_H
#define TIGER_MACHINE_H

void printBoard ( char a[][3] );

void createBoard( char a[][3] );

bool check( char a[][3] );

double tiger_machine( int money, double extra );

#endif
